# Permissions

Permissions vary by platform and features used.

## Camera / Photos

Used for inventory photo extraction (if enabled). The app will request access only when you attempt to select/take a photo.

## Network

The app requires network access to:
- authenticate
- sync inventory, required items, and plans
- run AI-assisted features (through the backend)

